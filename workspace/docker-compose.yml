# Crypto Strategy Lab — Local Development Infrastructure
# Run: docker-compose up -d

services:


  redis:
    image: redis:7-alpine
    container_name: csl-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
